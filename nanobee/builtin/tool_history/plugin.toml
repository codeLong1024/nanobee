[plugin]
name = "tool_history"
version = "1.1.0"
description = "历史消息管理工具：trim_history（粗暴截断）和 consolidate_history（智能压缩+归档）。纯机制，LLM 自主决定。"
author = "nanobee-team"
type = "tool"

[config]
enabled = true
